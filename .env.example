# Copy to .env for self-hosting with compose.yaml.

# Domain that points at this host (required for automatic HTTPS).
MCP_DOMAIN=mcp.example.org

# Optional: a real contact email enables the Unpaywall full-text source.
# OpenAlex/Crossref/Semantic Scholar work without it.
PURE_CONTACT_EMAIL=
